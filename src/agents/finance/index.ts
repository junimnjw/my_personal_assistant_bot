import type { DynamicStructuredTool } from "@langchain/core/tools";

export function createFinanceTools(_userId: number): DynamicStructuredTool[] {
  // TODO: 자산관리 도구 추가 (지출 기록, 수입 관리, 자산 조회 등)
  return [];
}
