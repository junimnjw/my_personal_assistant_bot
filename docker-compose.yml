services:
  personal-assistant:
    build: .
    container_name: my-personal-assistant
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
